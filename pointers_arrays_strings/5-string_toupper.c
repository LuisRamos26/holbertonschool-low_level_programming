#include"main.h"

/**
 * *string_toupper -Change lowercase to uppercase
 * @a: An input value
 * Return: 0
 */

char *string_toupper(char *)
{
	int i = 0;

	while (i < n)
	{
		n--;
		aux = a[i];
		a[i++] = a[n];
		a[n] = aux;
	}

}
