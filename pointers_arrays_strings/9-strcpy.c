#include"main.h"
#include <stdio.h>

/**
 * _strcpy -  prints n elements of an array of integers.
 * @dest: A char input
 * @src: A char input
 * Return: dest
 */

char *_strcpy(char *dest, char *src)
{

	while
	for (i = 0; i < n; i++)
	{
		printf("%d", a[i]);
		if (i < n - 1)
			printf(", ");
	}
	putchar('\n');
}
