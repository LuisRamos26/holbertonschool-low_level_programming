#include "main.h"

/**
 * _abs - check main.h
 * @value: an integer input
 * Description: returns absolute value of a number
 * Return: Absolute value
 */
int _abs(int value)
{
	if (value >= 0)
		return (value);
	else
		return (value * -1);
}
